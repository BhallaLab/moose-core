load_file("nrngui.hoc")
load_file("hoc/traubcon.hoc")
load_file("hoc/utility.hoc")
// load_file("fortmap.hoc")
load_file("cells/supbask_template.hoc")
strdef tstr
PI=3.141592
FARADAY=96485
secondorder=2

celsius=30
//till the shift bug in the mod files are fixed (table depends on range variable)
// usetable_naf2 = 0
// usetable_naf = 0
// usetable_napf_supbask = 0
// usetable_napf_tcr = 0

define_shape()
//objref runinfo_, choice_, cell, nil, stim1, stim2, mchoice_
objref cell, stim1, stim2
/* runcell(runtime, delay) */

proc runcell(){ local i, t1, a1, t2, a2 localobj vvec, tvec, f, g, caconc, ikvec
    print "--------------------------------------------------------------"
    print "|    NEURON Simulation of supbask cell                       |"
    print "--------------------------------------------------------------"
    tstop = $1
    t1 = $2
    t2 = $3
    cell = new supbask()
    access cell.comp[1]
    f = new File()
    tvec = new Vector()
    vvec = new Vector()
    caconc = new Vector()
    
    tvec.record(&t)
    vvec.record(&cell.comp[cell.presyn_comp].v(0.5))
    caconc.record(&cell.comp[1].cai(0.5))
    stim1 = new IClamp(0.5) 
    stim1.amp = 0.3
    stim1.del = t1
    stim1.dur = t2
    printf("Run for: %g ms, inject %g nA current at %g ms for %g ms\n", tstop, stim1.amp, stim1.del, stim1.dur)    
    run()    
    f.wopen("data/Vm_supbask.plot")
    for ii = 0, vvec.size() - 1{
    	f.printf("%g %g\n", tvec.x(ii), vvec.x(ii))
    }
    f.close()
    
    f.wopen("data/Ca_supbask.plot")
    for ii = 0, caconc.size() - 1{
    	f.printf("%g %g\n", tvec.x(ii), caconc.x(ii))
    }
    f.close()
    
    print "--------------------------------------------------------------"
    print "|    NEURON Simulation of supbask   cell finished            |"
    print "--------------------------------------------------------------"

}

proc finit0(){
    print "finit0"
    forsec cell.all { v = -65 }
}

runcell(200., 50., 50.)
quit()
